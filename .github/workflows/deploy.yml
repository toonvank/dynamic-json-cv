name: Deploy CV

on:
  push:
    branches: ["main"]

concurrency:
  group: deploy-cv
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to server directory
        run: |
          set -euo pipefail
          DEPLOY_DIR="/var/www/cv"
          echo "Deploying to $DEPLOY_DIR"
          sudo mkdir -p "$DEPLOY_DIR"
          sudo chown -R $USER:$USER "$DEPLOY_DIR"
          rsync -a --delete \
            --exclude '.git/' \
            --exclude '.github/' \
            --exclude 'node_modules/' \
            ./ "$DEPLOY_DIR/"
